// ----------------------------------// --------- MASONRY ----------------// ----------------------------------$(document).ready(function () {    $(function () {        let masonry = $(".my-masonry-grid");        let mediaQueryMovil = "(min-width: 1px) and (max-width: 767px)";        let mediaQueryTablet = "(min-width: 768px) and (max-width: 1284px)";        let mediaQueryEscritorio = "(min-width: 1285px)";        let breakPointMovil = window.matchMedia(mediaQueryMovil); // Inicializamos la mediaquery        let breakPointTablet = window.matchMedia(mediaQueryTablet); // Inicializamos la mediaquery        let breakPointEscritorio = window.matchMedia(mediaQueryEscritorio); // Inicializamos la mediaquery        if (breakPointMovil.matches) {            $(masonry).masonryGrid({                'columns': 1            });        } else if (breakPointTablet.matches) {            $(masonry).masonryGrid({                'columns': 2            });        } else if (breakPointEscritorio.matches) {            $(masonry).masonryGrid({                'columns': 3            });        }    });});